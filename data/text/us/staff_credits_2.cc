#include "staff_credits.hh"

// The preceding generated pointer table ends on a four-byte boundary in every
// baseline ROM, so the ordinary object alignment preserves this position.
char const gCppRuntimeBadAlloc_StaffCredits[] =
    "bad_alloc";

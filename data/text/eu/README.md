# EU text baseline

This tree is the EU region's independent text source location.  Its initial
contents are copied from the US source only to establish the normal regional
compilation path.  Replace each object from `baserom_eu.gba` as its ROM block,
pointer table, and call sites are matched.  Mary-managed RIFF script text is
intentionally outside this tree.

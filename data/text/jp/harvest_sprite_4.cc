#include "harvest_sprite.hh"

char const gCppRuntimeBadAlloc_HarvestSpriteMiniGamePostChickenFestival[] =
    "bad_alloc";

// func_080BE728 copies the two-byte Shift-JIS glyph rather than a numeric
// value, so JP keeps its original full-width zero here.
char const gText_080BE728DigitZero[] =
    "０";

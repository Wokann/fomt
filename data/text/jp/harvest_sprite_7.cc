#include "harvest_sprite.hh"

// func_080C38E4 copies the two-byte Shift-JIS glyph rather than a numeric
// value, so JP keeps its original full-width zero here.
char const gText_080C38E4DigitZero[] =
    "０";

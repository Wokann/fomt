#include "font.hh"

// This is an ordinary terminated C string. The following glyph object carries
// the required four-byte alignment.
extern char const gCppRuntimeBadAlloc_Font[] =
    "bad_alloc";

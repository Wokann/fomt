#include "field_render_runtime_data.hh"

// func_080B0708 copies exactly two bytes from this object. JP stores a
// full-width zero, followed by the ordinary C terminator.
char const gText_080B0708DefaultGlyph[] =
    "０";

#ifndef RANDOM_ITEM_TEXT_HH
#define RANDOM_ITEM_TEXT_HH

#include "prelude.h"

// Fallback name placed in a random-item result before a food or article is
// selected.  The linker preserves the original physical gap before the next table.
extern char const gText_RandomItem_NotAvailable[];

#endif // RANDOM_ITEM_TEXT_HH
